---
id: 010
title: Fresh work session
started: 2026-08-09
---

## 2026-08-09 16:16 — Kickoff
Goal for the session: Start a fresh journal session; the substantive work request has not yet been provided.
Current state of the world: `master` is clean at `3b82229`; phases 1–6 are complete, and the latest closed-session context leaves phase 7 as the remaining planned documentation and release work.
Plan: Wait for the user's actual request, inspect its exact target, work iteratively, and keep this session log current at meaningful checkpoints.

## 2026-08-09 16:19 — Security audit scoped
Goal for the session: Independently audit the `bigoci` package for security defects on `master` at `3b82229dd7938278eac6a197e5d1abf822382cef`.
Evidence rule: Report a finding only when an attacker-controlled path is reproducibly exploitable in the current code and produces a concrete consequence consistent with how `bigoci` is used; theoretical or best-practice-only concerns are not findings.
Scope and plan: Map the public-library trust boundaries and shipped CLI exposure, inspect the network/authentication, manifest/transfer/filesystem, error/logging, and dependency surfaces in parallel, run baseline checks, and independently reproduce every candidate before reporting it.

## 2026-08-09 17:18 — Security audit completed
Outcome: Six root findings survived public-API or shipped-CLI exploitation and consequence validation: off-origin upload sessions can cross the registry authentication boundary; Bearer realms permit private-IP SSRF and ambient Cookie Jar authority; a 4.3 KiB gzip manifest drove roughly 560 MiB RSS before the part-count guard; an attacker-owned regular partial in a shared non-sticky directory captured private artifact bytes across UIDs; a malicious sparse partial made resume hashing ignore cancellation for 24.6 seconds at 64 GiB; and CLI debug/error output leaked a redeemable realm query or accepted forged records/control bytes.
Strongest proof: A public zero-byte `Client.Push` replayed a live repository push bearer to the off-origin storage host, sent the Docker PAT to the storage-selected realm, and still returned success. The nonempty/default push path separately proved challenge-state poisoning and PAT exfiltration without direct initial Authorization forwarding.
Filesystem proof: In an isolated Linux container, uid 1002 pre-created a mode-0666 partial, uid 1001 completed the pull successfully, and uid 1002 read `private registry content` from the attacker-owned final file. The tested `1777` sticky-directory configuration was blocked by Linux `fs.protected_regular=2` and is not claimed vulnerable.
Verification: The 23-task `root:check` graph passed after clearing a stale lint cache; root, CLI, and benchmark module integrity checks passed; govulncheck and gosec results were manually reachability-reviewed; focused TLS proofs passed together; and transfer proofs were rerun on Darwin ARM64 and Linux ARM64. Scanner-only ORAS, Go ECH, `os.Root`, variable-path, and integer-conversion candidates were rejected when their vulnerable paths or consequences were not reachable.
Current target note: `origin/master` advanced during the audit to `54c5730d067cb28b02a49ac87122516c5d1d124e`, but `git diff 3b82229..54c5730 -- '*.go'` is empty. Transfer measurements remain pinned to `3b82229`; the focused auth, upload, realm, cookie, and CLI proofs were rerun with the exact ORAS 2.6.2 root/CLI module state from `54c5730` and still passed.
Artifact: The full evidence, prerequisites, remediation directions, rejected-candidate list, and verification record are in `/Users/josh/.codex/visualizations/2026/08/09/019fe8ce-613c-7b00-b7f4-722730668899/bigoci-security-audit.md`. No product code was changed.

## 2026-08-09 17:20 — Resolution plan approved
Decision: Resolve the six confirmed root findings as six narrow, regression-backed security fixes. The off-origin upload and manifest allocation findings are the first review priority; the file partial and resume-cancellation fixes are independent; the Bearer-realm fix follows the shared clean-client seam; CLI output sanitization remains isolated from the frozen field grammar.
Delivery: Build independent fixes in separate Worktrunk worktrees from current `origin/master` at `54c5730`, prove each minimized exploit fails on the parent and passes on the fix, run focused race tests plus the complete `root:check` graph, and integrate only through human-owned GitHub squash PRs. Keep the full exploit harnesses private.
Release gate: All six findings block the still-open first-release PR #11. After integration, rerun every original exploit harness, the Linux cross-UID and bounded-memory proofs, GHCR conformance, and final module/security checks before allowing `v0.1.0`.

## 2026-08-09 17:44 — First remediation slice under review
Delivery: PR #41 isolates off-origin upload sessions from registry authentication, ambient cookies, and storage response details; its focused exploit replays, full 23-task check graph, hosted CI, CodeQL, and Pages checks pass. PR #42 makes resume hashing context-aware between bounded reads; its deterministic regression proves one disk read, zero blob requests, no commit, and a reachable `context.Canceled`, and independent review found no actionable issue.
Realm boundary: The follow-on Bearer fix rejects registry-selected local/private IP literals unless they are the registry's own literal address, sends token exchanges through the Jar-free client, and applies the same address rule to upload locations. Both original loopback credential/secret exfiltration and ambient-cookie token reflection harnesses now fail at the intended boundary; the full local check graph passes.
Review correction: Independent review found that the first streaming-manifest prototype could still allocate hundreds of bounded slices through duplicate `layers` members. That branch has not been committed; it is back in implementation with the original under-4 MiB duplicate-member payload as a required allocation regression.

## 2026-08-09 18:04 — Three remediations integrated
Merged: PR #41 isolates off-origin upload sessions, PR #42 makes resume verification cancellable, and PR #43 rejects attacker-owned/permissive Unix partials before any registry request. Every exact reviewed head passed the complete hosted CI, CodeQL, and Pages gates before GitHub squash merge. The file fix also passed a real Linux ARM64 two-UID container test; its first hosted run caught only two Linux-only `usetesting` lint warnings, resolved with scoped explanations because `t.TempDir` is deliberately not traversable by the helper UID.
Manifest correction: The redesigned preflight plus standard unmarshal reduces the exact 4,193,960-byte, 341-duplicate-array proof from roughly 830 MiB allocated to 1.97 MiB while preserving legal last-member behavior. It remains unpublished pending a fresh independent adversarial review.
Deeper review: The Bearer prototype still allowed DNS names that dialed loopback and leaked redeemable realm state through validation errors. The CLI prototype removed raw challenge headers and terminal controls but still logged valid realm credentials embedded in token URL paths and repeated malformed challenges or token paths in final errors. Both branches remain uncommitted while enforcement moves to the outbound connection and library error boundaries and debug URLs become origin-aware placeholders.

## 2026-08-09 18:27 — Manifest allocation remediation integrated
Review correction: Fresh adversarial review bypassed the layer-count preflight with one legal descriptor carrying 1,397,950 `urls` entries, allocating roughly 138 MiB, and separately measured roughly 49 MiB for 300,000 unique manifest annotations. The unpublished decoder was replaced with a smaller wire representation that materializes only fields bigoci consumes and bounds each `layers` array in its JSON decoder.
Evidence: On the merged design, the original 1,397,950-layer payload, the descriptor-URL bypass, the annotation-map payload, and the malformed-tail payload each allocate under 1 KiB per decode. The exact 4,193,960-byte payload containing 341 individually legal layer arrays allocates about 590 KiB. Case-folded and escaped layer member names, duplicate/null behavior, and deliberately rejecting any individually oversized earlier duplicate are regression-pinned.
Merged: PR #46 passed the full 23-task local check graph, a clean independent rereview, and exact-head hosted CI, CodeQL, and Pages checks before GitHub squash merge as `cab0dd4`.

## 2026-08-09 20:04 — Registry-selected authority remediation integrated
Review corrections: The network/error fix moved from literal-IP checks to a default-deny outbound boundary that checks the direct connection peer before HTTP bytes leave and refuses opaque dial, proxy, protocol, and transport paths unless the caller explicitly opts out. Final adversarial passes additionally closed DNS-to-loopback routing, malformed Location and realm-ticket error reflection, peer-address reflection, reusable Bearer/Basic reflection through status bodies and manifest fields, and transport-cause identity loss. The explicit `WithUnverifiedExternalTransport` escape hatch now uses the caller's original transport so `RegisterProtocol`, proxy, dial, and pooling behavior are preserved while the caller owns that destination boundary.
Integration evidence: The rebased commit retained the bounded wire-manifest decoder and cancellation-aware resume hashing. The exact 4 MiB single-array, duplicate-array, descriptor-URL, annotation-map, and malformed-tail fixtures stayed below roughly 591 KiB allocated per decode; resume cancellation passed ten race runs. Public consequence probes reported zero realm/upload requests to mapped loopback, no leaked redeemable realm or malformed-Location ticket, no reflected peer address, and preserved `errors.Is`/`errors.As` identity without rendering cause text.
Merged: PR #49 passed all 18 local checks with an isolated golangci cache, full uncached race tests, conformance-tag compilation, Windows vet/build, two independent post-rebase reviews, and exact-head hosted CI, CodeQL, and Pages checks before GitHub squash merge as `f73bcf0`.

## 2026-08-09 20:42 — CLI output remediation integrated
Review corrections: Adversarial replay tests expanded the output boundary beyond raw challenges and terminal controls. Server-issued upload and redirect capabilities, digest-shaped Bearers, reflected response headers and transport errors, exact pull progress totals, completed and wire byte counts, successful pull sizes, and resumed `Range` offsets each carried an independently redeemable credential or capability in the real CLI before correction. The final design renders peer-selected response metadata, transport failures, distribution identifiers, Location-derived targets, pull byte metrics, and request ranges as structural markers while retaining stable field order, request classification, status values, and locally derived push counters. Location tracking uses fixed-size identities so debug redaction cannot retain attacker-sized paths.
Evidence: Every exploit regression first authenticates through the shipped CLI, proves the registry or storage received the credential or capability, and independently redeems the value for HTTP 200. On the fix, those same values are absent from debug, progress, summary, and final-error output; terminal controls, forged records, bidi text, and invalid UTF-8 are escaped without changing exit classification. Two independent exact-head reviews found no remaining proven high-value output channel.
Merged: PR #51 passed all 18 local checks, repeated race runs of the authenticated progress and `Range` replay regressions, the full adversarial overlay matrix, Windows and conformance compilation, hosted CI, CodeQL, Pages, and live GHCR conformance before GitHub squash merge as `0a80682`. The post-merge 18-task check and the three strongest replay regressions also pass on `master`.

## 2026-08-09 20:49 — Final remediation matrix passed
Integrated result: The BOCI-01 through BOCI-05 consequence regressions and eleven BOCI-06 credential, capability, and terminal-output regressions each passed three race-enabled runs on exact merged `master` at `0a80682`. The original private-realm probe made no realm request or credential disclosure; the 64 GiB sparse-resume probe returned at cancellation in zero reported delay instead of roughly 24.6 seconds; and the pinned CLI log-forgery probe emitted neither the hostile body, forged record, nor OSC payload.
Resource proof: The 1,397,950-layer decoder fixture remained at 603–607 bytes allocated per operation. The original 4,397-byte gzip network response expanded to the same 4,194,302-byte manifest but was rejected with 9.7 MiB total allocation instead of roughly 864.7 MiB. The Darwin run also passed the portable unsafe-partial and Unix permission regressions; the Linux-only cross-UID test remains covered by its earlier container proof and hosted Linux suite.
Final state: `master` and the journal branch are clean, the squash tree matches the independently reviewed feature tree, merged-master CI, CodeQL, Pages, and Release Please are green, and no reproduced exploit remains. Release PR #50 is intentionally left open as the separate 0.1.1 publication decision.

## 2026-08-09 20:56 — Close
Outcome: Session 010 met its goal. The independent audit admitted six exploit-proven root findings, and PRs #41, #42, #43, #46, #49, and #51 merged their consequence-backed remediations. Exact merged `master` at `0a80682` passed the final race-enabled exploit matrix, original harness reruns, full local checks, hosted CI/CodeQL/Pages, and live GHCR conformance with no reproduced exploit remaining.
Handoff: `master` is fast-forwarded and clean, all six implementation worktrees and local feature branches were removed after their merged PRs were verified, and the durable authority, decoding, partial/resume, and CLI-output constraints were promoted to `TECH_NOTES.md`. PR #50 remains open as the separate v0.1.1 publication decision.
